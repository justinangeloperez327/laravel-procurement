name: rounds-format-once

on:
  push:
    branches:
      - feat/procurement-rounds
    paths:
      - .github/workflows/rounds-format-once.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/procurement-rounds

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install frontend dependencies
        run: npm ci

      - name: Apply repository formatter
        run: npm run check -- --fix

      - name: Commit formatter output and remove helper
        run: |
          rm .github/workflows/rounds-format-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add resources/js/pages/procurement/rounds/index.tsx .github/workflows/rounds-format-once.yml
          git diff --cached --quiet || git commit -m "style: format procurement rounds workspace"
          git push
